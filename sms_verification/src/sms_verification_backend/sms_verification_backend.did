type Response = record { message : text; success : bool };
service : {
  set_encryption_key  : (text) -> (Response);
  configure_twilio    : (text, text, text) -> (Response);
  send_sms            : (text) -> (Response);
  verify_otp          : (text, text) -> (Response);
  get_verified_phones : () -> (vec text);
}
